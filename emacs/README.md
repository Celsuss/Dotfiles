
# Emacs custom config

## Run emacs with this config
``` bash
emacs --init-directory=~/workspace/dotfiles/emacs
```

It's recommended to setup an alias for this:
``` bash
alias emacs-dev="emacs --init-directory=~/workspace/dotfiles/emacs"
```
